# Copy to inventory.ini and fill in the VM's public IP (terraform output vm_external_ip).
# inventory.ini is gitignored - never commit it.

[vm]
zero-to-prod ansible_host=YOUR_VM_IP ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_ed25519
