apiVersion: v1
kind: Service
metadata:
  name: zero-to-prod-app
  labels:
    app: zero-to-prod-app
spec:
  selector:
    app: zero-to-prod-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
